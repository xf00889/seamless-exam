databases:
  - name: exammaker-db
    plan: free
    databaseName: exammaker
    user: exammaker_user

services:
  - type: web
    name: localexammaker
    runtime: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "gunicorn exam_system.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: exammaker-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DJANGO_SETTINGS_MODULE
        value: exam_system.settings_production
      - key: ALLOWED_HOSTS
        value: .onrender.com
      - key: PYTHON_VERSION
        value: "3.11.6"
      - key: WEB_CONCURRENCY
        value: "4"
